interface:
  display_name: "AFTER FAST FRB Processing"
  short_description: "Run AFTER post-search FAST FRB processing."
  default_prompt: "Use $fast-frb-observation-processing as the AFTER protocol to process one FAST FRB observation from raw, cut, calibrated, or already detected data through label review and results table export."
