interface:
  display_name: "FAST FRB Processing"
  short_description: "Process one FAST FRB observation."
  default_prompt: "Use $fast-frb-observation-processing to process one FAST FRB observation from raw, cut, calibrated, or already detected data through label review and results table export."
